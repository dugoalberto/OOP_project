//
// Created by Alberto Dugo on 09/05/23.
//

#include "SpedizioneWidget.h"
/*
SpedizioneWidget::SpedizioneWidget(const SpedizionePremium &spedizione, QWidget *parent): QWidget(parent), m_spedizione(spedizione){
// Creo i widget per mostrare i dettagli della spedizione
    auto* labelTrakingNumber = new QLabel(QString::number(m_spedizione.getTrakingNumber()), this);
    auto* labelDestinatario = new QLabel(QString::fromStdString(m_spedizione.getDestinatario().getNomeCognome()), this);
    auto* labelStato = new QLabel(QString::fromStdString(m_spedizione.getStato().getDescStato()), this);
    auto* labelCosto = new QLabel(QString::number(m_spedizione.getCosto()), this);

    // Creo un layout per la riga della spedizione
    auto* layout = new QHBoxLayout(this);
    layout->addWidget(labelTrakingNumber);
    layout->addWidget(labelDestinatario);
    layout->addWidget(labelStato);
    layout->addWidget(labelCosto);
}
*/